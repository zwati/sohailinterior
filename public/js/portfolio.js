// Portfolio Page Specific Logic (Currently placeholder/empty state initialization)

document.addEventListener("DOMContentLoaded", () => {
  // Trigger reveal check if custom animations exist on load
  if (window.revealCheck) {
    window.revealCheck();
  }
});
